{
  "name": "@bookstack-mcp/core",
  "version": "2.5.0",
  "description": "Shared BookStack API client and types for BookStack MCP",
  "type": "module",
  "main": "dist/bookstack-client.js",
  "types": "dist/bookstack-client.d.ts",
  "exports": {
    ".": {
      "import": "./dist/bookstack-client.js",
      "types": "./dist/bookstack-client.d.ts"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "type-check": "tsc --noEmit",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@fast-check/vitest": "^0.3.0",
    "@vitest/coverage-v8": "^4.1.2",
    "fast-check": "^4.5.3",
    "typescript": "^6.0.2",
    "vitest": "^4.0.18"
  },
  "engines": {
    "node": ">=18"
  }
}
